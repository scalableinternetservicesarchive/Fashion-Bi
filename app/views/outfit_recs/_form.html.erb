<%= form_for(outfit_rec) do |f| %>
  <% if outfit_rec.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(outfit_rec.errors.count, "error") %> prohibited this outfit_rec from being saved:</h2>

      <ul>
      <% outfit_rec.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :clothes_ids %>
    <%= f.text_field :clothes_ids %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
